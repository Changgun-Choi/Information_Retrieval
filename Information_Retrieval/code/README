README

preprocess: helper function for preprocessing: preprocess(sentence) 

feature_computation: computes bm25 and bert sentence embeddings (based on contextualized word embeddings) for a small training sample. idf data is needed to run.

bert_embeddings: Defines a function to compute the similarity based on bert embeddings between a query and a passage.
 
